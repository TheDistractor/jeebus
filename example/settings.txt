# lines in this file are of the form: <name> "=" <json-value>
# these values can be overridden via environment variables

APP_DIR    = "../app"
BASE_DIR   = "../base"
COMMON_DIR = "../common"
DB_DIR     = "../db"
FILES_DIR  = "../files"
LOGGER_DIR = "../logger"
